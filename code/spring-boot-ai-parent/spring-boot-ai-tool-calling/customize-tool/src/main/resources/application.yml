server:
  port: 10006
spring:
  application:
    name: spring-boot-ai-agent
  ai:
    dashscope:
      api-key: ${AI_DASHSCOPE_API_KEY}


logging:
  level:
    org.springframework.ai.chat.client.advisor: DEBUG
