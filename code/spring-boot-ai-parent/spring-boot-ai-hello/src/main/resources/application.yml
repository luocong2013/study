server:
  port: 18080
spring:
  application:
    name: spring-boot-ai-hello
  ai:
    dashscope:
      api-key: ${AI_DASHSCOPE_API_KEY}


logging:
  level:
    org.springframework.ai.chat.client.advisor: DEBUG
